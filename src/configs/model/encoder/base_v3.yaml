_target_: components.v3.discussion_transformer_simple.DiscussionTransformerV3

embedding_dim: 768

graph_stack_factory:
  _target_: components.v2.graph_encoder_layer.BaseGraphTransformer
  _partial_: True
  num_layers: 3
  graph_tfmr_factory:
    _target_: components.v2.graph_encoder_layer.GraphTransformerBlock
    _partial_: True
    n_heads: 12
    n_kv_heads: 12
    dim: "${model.encoder.embedding_dim}"
    ffn_dim: 3072
    differential_attention: False
    use_rope: False
    rope_mixed: True

num_out_degree: 5
use_out_degree_emb: False

block_size: 150
